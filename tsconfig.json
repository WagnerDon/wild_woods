{
 "compilerOptions": {
  "target": "ES2015", // es version
  "module": "ES2015", // module system
  "strict": true, // higher program correctness
  "outDir": "./dist/js", // where to compile it to
  "sourceMap": true // helps to debug ts code
 },
 "include": ["./src/**/*.ts"] // what files to compile
}
